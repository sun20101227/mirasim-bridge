# 用量统计与费用估算（0.8.7）

控制台新增 **用量统计** 页面。顶部“当前账号”选择 main、托管账号或独立容器账号，分别查看其记录。无需在 sub2 更改配置。

1. 选择今天、近 7 天或近 30 天（按 UTC 自然日，包含今天），再选择模型。
2. 查看模型请求次数、失败数、输入/输出 Token、缓存读写、每日趋势，以及最近请求的耗时和用量完整度。
3. 需要估算费用时，在“模型单价”输入准确模型 ID，以及每百万 Token 的 USD 价格，点击保存。未知项留空，零表示明确免费；全留空再保存可删除该模型价格。可从已有记录选择模型，也可手动输入尚未调用的模型。
4. “导出模型汇总 CSV”和“导出最近明细 CSV”下载当前页面的本地快照，不触发上游查询或模型推理。CSV 使用带 BOM 的 UTF-8，并防止模型名称被表格软件解释成公式。

## 统计范围

- 从安装包含此功能的版本后开始记录，不能补回旧版未采集的用量。
- 统计已通过本桥接器发往上游的 Messages、Responses 和 Responses compact 请求。模型测试、自动窗口任务所发的小请求也计入；目录、会员、额度查询与 `count_tokens` 不计入推理用量。
- 本地鉴权失败、限流、禁用模型等未转发请求不记入本页；上游失败或断流记录为失败，并保留已收到的部分用量。
- 一次桥接请求算一次记录；明确错误触发的自动重试增加“上游尝试次数”。Token 来自该请求被观察到的响应，不推测上游未报告的重试扣费。
- 请求日以记录完成时的 UTC 日期为准；明细时间以浏览器时区显示。模型筛选同时匹配请求模型和上游返回的模型。返回模型不同会明确显示，统计不会改写模型回复。
- 每天最多保留 1024 种“请求模型、返回模型、协议”组合的汇总，页面最多展示 1024 个汇总行。超限会提示统计不完整。明细最多展示所选范围内最近 100 条；模型筛选作用于每天最近 100 条，而汇总覆盖当天所有有效记录。

## Token 与费用口径

- Messages 的总输入为普通输入 + 缓存读取 + 缓存写入。流式用量是累计快照，重复事件不重复相加。[Claude 官方说明](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)
- Responses 的输入包含缓存读取，输出包含推理 Token；缓存和推理数作为分项展示，不再重复加入总数。[OpenAI Responses 文档](https://developers.openai.com/api/reference/resources/responses/methods/retrieve)
- **完整**：收到结束事件/完整 JSON，且包含有效的输入、输出数值；**不完整**：只收到部分用量、断流或字段异常；**未知**：没有收到可用的 usage 字段。已知 Token 合计可能低于实际消耗，不能视为官方账单。
- 只有完整用量且所有非零消耗项目均配置了单价，才计算该次费用。未定价、缺字段和无 usage 的请求不会显示为零费用；汇总注明已估算请求占比。
- 费用按普通输入、输出、缓存读取、缓存写入分别计算，单位 USD / 1,000,000 Token。有返回模型时使用该模型价格，否则使用请求模型价格。价格保存只作用于后续记录，历史估算不重算。
- 缓存写入目前使用统一单价，不区分缓存 TTL；长上下文阶梯价格、批处理优惠、工具调用、图片等额外费用不在此估算内。
- **估算费用不是 Mirasim 实际扣款，也不是 sub2 的消费账单。Mirasim 的 5h/7d 额度使用其自身单位，不能从 Token 或美元价格直接换算；剩余额度仍见账号页面。**

## 保存与部署

- 每个账号的 `config.json` 同目录新增 `usage/`，每日一个 JSONL 文件，单价为 `usage/prices.json`。托管账号位于 `profiles/<账号>/usage/`，独立容器在自己的数据卷中。
- 保留最近 30 个 UTC 自然日，读取或记录时清理过期日文件。每天最多 8 MiB 用量日志（每账号约 240 MiB 上限，不含少量单价配置）；写入失败、容量受限、损坏行会显示提示，不阻止推理回复。漏记数只统计当前进程，不能据此追溯停机期间的缺口。
- 记录只包含时间、模型、协议、状态、耗时、Token 和费用估算，不保存提示词、回复正文、密钥或凭证；无需额外网络调用。数据目录已经加入 Git 忽略规则，并排除在镜像和源码包之外。
- 正常容器重启、保留数据卷的升级后记录仍在。突然断电/强制终止可能丢失未完成写入；损坏行跳过并提示。不要让多个 bridge 进程同时写同一账号数据目录。
- 网页仍按当前页局部自动更新，编辑中的单价不会被轮询覆盖。切换账号后重新选择该账号的模型。
- 已安装网页升级服务时，在“版本与升级”点击“强制检查更新”，再点击“升级至最新发布”。同一次升级更新 bridge 和宿主机后台；成功后刷新页面即可看到“用量统计”。若发布源被固定在旧版，先点击“改为跟随最新发布”。仅发布 GitHub 不会自动更新你的服务器。
