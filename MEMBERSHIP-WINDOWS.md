# 会员状态与自动额度窗口（0.8.6）

## 会员到期状态

概览的每个账号行显示当前套餐与到期状态；“账号管理 → 会员到期状态”显示精确时间，支持手动刷新。默认每 15 分钟从 `https://auth.mirasim.ai/auth/me` 查询一次（沿用该账号配置的认证服务），字段是 `plan` / `plan_exp`。时间按浏览器本地时区显示。

目前真实接口为每个账号返回一个当前套餐，不会虚构该账号同时持有多个会员。多个 Mira 账号分别显示。缺少期限、null 或 0 显示“上游未提供到期时间”，不当成永久会员；查询失败保留历史日期并标记过期。会员期限与登录令牌的 `exp` 完全分开。

已开启额度备注同步的账号，会在下次 `/v1/limits` 采样时把会员与额度写到 sub2 账号备注。sub2 后台勾选“备注”列即可看到，原版计费余额和限额不修改。原始登录 token、设备私钥、邮箱不会出现在会员状态中。

## 开启自动窗口

1. 升级到 0.8.6 后刷新网页，进入“账号管理”，顶部选择具体 Mira 账号。
2. 在“自动开启额度窗口”选择当前账号已启用的短请求模型（建议使用自己的可用 Haiku 型号），勾选 5h / 7d，确认每天请求上限。
3. 勾选“为当前账号启用自动窗口”，保存。**默认关闭，新增账号不会继承其他账号的启用状态。**
4. 后台在健康循环中检查，约每分钟最多执行一次判断；默认额度采样 5 分钟，触发前另外读取实时额度。也可点“检查并按需启动”，不会无条件发送。
5. 查看执行记录中的模型、目标窗口、时间及结果。关闭开关保留历史防重复记录。

这不是强制重置或延长额度：只有上游以第一笔请求开始计时，短请求才可能启动新窗口；实际窗口时长由 Mirasim 决定，不会凭本地时间伪造上游重置时间。5h/7d 同时需要启动时只发送一次。模型专用窗口如 `7d_fable` 不会被错误当成另一轮账号窗口。

## 调度与费用

- 只识别上游真正返回的 `5h` / `7d`，不为没有的窗口补造周期。
- 重置过期至少 30 秒才触发；用量为零但重置时刻稳定在未来，说明窗口正在计时，不发送。如果上游显示的空闲 reset 随查询时间移动，至少两次相隔 30 秒的有效观察后才允许启动。
- 发送前重新查询额度、检查模型目录和账号状态。真实业务已启动窗口时跳过。手动暂停、调度关闭、忙碌、限流退避、会员过期、额度未知或耗尽时不发送。
- 请求文本固定 `hi`，走本账号的实际 bridge `/v1/messages`，设置 `max_tokens=16`。不使用会移除输出上限的 Responses 自动探测；无法用 Messages 的型号可选其他可用型号。
- 小请求仍消耗 Mirasim 上游额度，且包含协议要求的系统内容，**不是保证只花一个 token**。它从 bridge 内部发出，通常不会计入 sub2 用户账单。
- 默认每个账号最多 6 次/滚动 24 小时，可调 1–8 次；两次发送至少间隔 5 分钟。4xx/5xx 明确失败按 10 分钟至 2 小时退避；超时/断流结果不明则按完整窗口冷却，避免重复计费。
- 自动任务不会更改用户真实请求的模型、指纹或内容；已有转发约束保持不变。

## 状态与持久化

| 状态 | 含义 |
| --- | --- |
| 等待窗口 | 未到期，或还没有空闲证据 |
| 请求成功，等待确认 | 完整流已返回，但不能据此断言计时开启 |
| 复查确认窗口已在计时 | 观察到用量与新 reset，或 15 分钟后 reset 稳定在请求时间加窗口长度附近 |
| 尚未确认窗口 | 15 分钟后仍无足够证据；等完整周期冷却，不反复发请求 |
| 请求结果不确定 | 超时或断流；保留发送记录，不能当作未发送重试 |
| 状态不可读/中断任务的锁 | 已停止自动请求，需恢复记录或确认旧任务已停止后处理锁 |

每个 profile 的配置目录下保存 `window-keeper.json` 和短期 `.lock`。先原子写入发送记录并落盘，再发出请求。同一 profile 多进程争用锁时只有一个发送；同一 bridge 中同一 Mira 身份的重复任务会被拦截。

不要在两台服务器或两份独立数据卷上同时为同一个 Mira 账号开启任务。共享文件锁无法协调独立的存储。同样不要和另一个自动窗口工具重复管理同一账号。

正常关闭会释放锁。强制杀进程可能留下 `.lock`，程序不会擅自删除另一进程的锁；需先停用任务并确认旧进程已停止，再由管理员处理。状态损坏或丢失时也不会悄悄新建历史，以免重复消耗。

## 验证范围

真实 `/auth/me` 和 `/v1/limits` 均返回 200，套餐与期限字段已核对；经本地实际桥接器发送 `claude-haiku-4-5` 的一次 `hi`，HTTP 200，收到文本及 `message_stop`。

该真实账号的 5h/7d 本来已在计时，因此**没有宣称实测启动了空闲的 5h/7d**。窗口到期/双窗口合并、后续确认、重启防重复、写盘失败、竞态、额度阻塞等通过模拟时钟与上游测试；生产账号空闲窗口的实际启动效果仍需启用后看执行记录确认。没有给已有生产账号自动打开开关。

调度策略参考用户提供的 `sub2api-quota-keeper` / `sub2api-quota-native` 本地项目的防重复、预检、预算和验证思路；未复用其 OpenAI OAuth 额度字段或修改 Quota Keeper 插件。
